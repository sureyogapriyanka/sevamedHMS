# Backend API base URL (used by proxy and frontend)
VITE_API_BASE_URL=https://sevamed-backend.onrender.com/api

# Optional base path for routes (leave empty if app is at root)
VITE_BASE_PATH=

# Environment (vite automatically injects this as import.meta.env.MODE)
VITE_NODE_ENV=development
